// src/modules/sync/sync.enums.ts

export enum SyncEntity {
  MATTERS = 'MATTERS',
  CONTACTS = 'CONTACTS',
  BILLS = 'BILLS',
  TASKS = 'TASKS',
  DOCUMENTS = 'DOCUMENTS',
  ACTIVITIES = 'ACTIVITIES',
}

export enum SyncStatus {
  RUNNING = 'RUNNING',
  COMPLETED = 'COMPLETED',
  FAILED = 'FAILED',
  PARTIAL = 'PARTIAL',
}
